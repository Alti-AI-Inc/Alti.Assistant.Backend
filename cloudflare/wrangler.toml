name = "aphura-edge"
main = "worker.js"
compatibility_date = "2024-09-01"

[vars]
BACKEND_ORIGIN = "https://api.aphurahq.com"

# To deploy:
# 1. npm install -g wrangler
# 2. wrangler login
# 3. cd cloudflare && wrangler deploy
